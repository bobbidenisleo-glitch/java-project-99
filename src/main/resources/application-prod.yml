spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver
    hikari:
      data-source-properties:
        port: 5432
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
